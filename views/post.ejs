<!DOCTYPE html>
<html lang="zh-TW">
<head>
    <meta charset="UTF-8">
    <title>投稿餐廳</title>
    <link href="https://fonts.googleapis.com/css2?family=Noto+Serif+TC:wght@400;600&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/index.css">
    <link rel="stylesheet" href="/post.css">
</head>
<body>
    <nav><a href="/" class="logo">食光地圖</a></nav>
    <div class="post-box">
        <h2>📮 投稿新餐廳</h2>
        <label>餐廳名稱</label><input type="text" id="pName">
        <label>詳細地址</label><input type="text" id="pAddr" placeholder="台北市信義區...">
        <label>聯絡電話</label><input type="text" id="pPhone">
        <label>美食圖片網址</label><input type="text" id="pImg" placeholder="貼上 jpg/png 圖片連結">
        <label>推薦原因</label><textarea id="pDesc"></textarea>
        <button onclick="publish()" class="btn-publish">確認發布</button>
    </div>

    <script>
        async function publish() {
            const name = document.getElementById('pName').value.trim();
            const addr = document.getElementById('pAddr').value.trim();

            // ✅ 前端防呆：至少要有名稱+地址
            if (!name || !addr) {
                alert('請至少填寫「餐廳名稱」與「詳細地址」');
                return;
            }

            const data = {
                name,
                address: addr,
                phone: document.getElementById('pPhone').value.trim(),

                // ⚠️ 你目前用 substring 切縣市/區：只適合「台北市信義區」這種固定長度
                // 先保留你的寫法（能用就好），之後你想再改成更準的方式我再幫你升級
                city: addr.substring(0, 3),
                district: addr.substring(3, 6),

                image_url: document.getElementById('pImg').value.trim(),
                description: document.getElementById('pDesc').value.trim()
            };

            const res = await fetch('/api/restaurants', {
                method: 'POST',
                headers: { 'Content-Type': 'application/json' },
                body: JSON.stringify(data)
            });

            // ✅ 不管成功/失敗，都嘗試讀訊息
            let result = {};
            try {
                result = await res.json();
            } catch (e) {
                // 如果後端沒回 JSON，避免整個爆掉
                result = { success: false, message: '伺服器回應格式錯誤' };
            }

            // ✅ 用 res.ok + success 雙保險
            if (!res.ok || !result.success) {
                alert(result.message || '投稿失敗，請稍後再試');
                return;
            }

            alert(result.message || '投稿成功！');
            location.href = '/';
        }
    </script>
</body>
</html>
